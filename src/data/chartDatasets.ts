// Stub for chart datasets
import type { Dataset } from '../types/charts';

export const SAMPLE_DATASETS: Dataset[] = [];

export function getDataset(id: string): Dataset | undefined {
  return undefined;
}
